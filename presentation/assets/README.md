# Visual Assets

Use `report/figures/encoding_visualisation.png` as the static gameplay visual for the methodology slide.

If you want a short demo clip instead, record one self-play or evaluation game after running:

```bash
python3 scripts/run_smoke_suite.py
```

and capture the representative move log from `artifacts/smoke_suite/`.
